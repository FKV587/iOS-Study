### **📌 iOS 开发中常用的设计模式**
iOS 开发中，常见的设计模式主要包括 **创建型模式、结构型模式和行为型模式**。  

---

## **1️⃣ 创建型模式（Creational Patterns）**
🔹 **主要作用**：封装对象的创建方式，避免直接初始化，提升灵活性。  

### **① 单例模式（Singleton）**
**🔹 作用**：保证一个类**全局只有一个实例**，适用于**全局管理对象**（如网络请求管理、用户管理）。  
**🔹 iOS 框架中的应用**：`UIApplication.shared`、`UserDefaults.standard`、`FileManager.default`。  

**🔹 实现（Swift 线程安全单例）**：
```swift
class NetworkManager {
    static let shared = NetworkManager()
    private init() {}  // 禁止外部实例化
}
```

---

### **② 工厂模式（Factory）**
**🔹 作用**：用于创建**相同父类/协议**的对象，解耦对象的创建过程。  
**🔹 适用于**：不同子类对象的创建，如**UIButton 的不同类型**（`system`、`custom`）。  

**🔹 实现（简单工厂模式）**：
```swift
class ButtonFactory {
    static func createButton(type: UIButton.ButtonType) -> UIButton {
        return UIButton(type: type)
    }
}
let button = ButtonFactory.createButton(type: .system)
```

---

## **2️⃣ 结构型模式（Structural Patterns）**
🔹 **主要作用**：简化代码结构，提高模块复用性。

### **③ 适配器模式（Adapter）**
**🔹 作用**：**适配不兼容的接口**，常用于**旧代码迁移**或**第三方 SDK 兼容**。  
**🔹 适用于**：UIKit 和 SwiftUI 互相适配、Objective-C 和 Swift 代码兼容。  

**🔹 实现（适配不同的数据模型）**：
```swift
protocol JSONCompatible {
    func toJSON() -> [String: Any]
}

class UserModel: JSONCompatible {
    var name: String
    init(name: String) { self.name = name }
    
    func toJSON() -> [String: Any] {
        return ["name": self.name]
    }
}
```

---

### **④ 代理模式（Delegate）**
**🔹 作用**：用于**对象间的解耦通信**，一种**一对一**的关系。  
**🔹 iOS 框架中的应用**：`UITableViewDelegate`、`UICollectionViewDelegate`。  

**🔹 实现（自定义 Delegate）**：
```swift
protocol CustomDelegate: AnyObject {
    func didTapButton()
}

class ViewController: UIViewController {
    weak var delegate: CustomDelegate?

    func buttonAction() {
        delegate?.didTapButton()
    }
}
```

---

## **3️⃣ 行为型模式（Behavioral Patterns）**
🔹 **主要作用**：管理对象之间的交互方式。

### **⑤ 观察者模式（Observer）**
**🔹 作用**：当**一个对象的状态发生变化时，所有监听者都会收到通知**（**一对多**）。  
**🔹 iOS 框架中的应用**：`NotificationCenter`、`KVO`、Combine 的 `@Published`。

**🔹 实现（NotificationCenter）**：
```swift
NotificationCenter.default.post(name: NSNotification.Name("UserLoggedIn"), object: nil)

NotificationCenter.default.addObserver(self, selector: #selector(userLoggedIn), name: NSNotification.Name("UserLoggedIn"), object: nil)
```

---

### **⑥ 责任链模式（Chain of Responsibility）**
**🔹 作用**：**多个对象依次处理请求**，如果当前对象无法处理，就传递给下一个对象。  
**🔹 适用于**：事件响应链（`UIResponder` 事件传递）、拦截请求（如 **URLProtocol**）。  

**🔹 实现（责任链模式拦截器）**：
```swift
class Logger {
    var next: Logger?
    
    func log(_ message: String) {
        print("Default Logger: \(message)")
        next?.log(message)
    }
}
```

---

### **⑦ 策略模式（Strategy）**
**🔹 作用**：定义一组**可替换的策略**，根据不同情况动态切换。  
**🔹 适用于**：网络请求策略、缓存策略、排序算法等。  

**🔹 实现（定义不同支付方式）**：
```swift
protocol PaymentStrategy {
    func pay(amount: Double)
}

class Alipay: PaymentStrategy {
    func pay(amount: Double) {
        print("使用支付宝支付 \(amount) 元")
    }
}

class WeChatPay: PaymentStrategy {
    func pay(amount: Double) {
        print("使用微信支付 \(amount) 元")
    }
}

class PaymentContext {
    private var strategy: PaymentStrategy
    init(strategy: PaymentStrategy) { self.strategy = strategy }
    
    func executePayment(amount: Double) {
        strategy.pay(amount: amount)
    }
}
```
调用：
```swift
let payment = PaymentContext(strategy: Alipay())
payment.executePayment(amount: 100)
```

---

## **4️⃣ 结合实际开发，如何选择？**
- **单例模式** 👉 适用于全局管理，如 `NetworkManager`、`UserDefaults`。  
- **工厂模式** 👉 适用于创建不同类型的 UI 组件。  
- **代理模式** 👉 适用于 `UITableView` 和 `UICollectionView` 的事件处理。  
- **观察者模式** 👉 适用于 `NotificationCenter` 或 `KVO` 监听数据变化。  
- **策略模式** 👉 适用于可变的支付、网络、缓存策略。  
- **适配器模式** 👉 适用于 Swift 和 Objective-C 代码兼容。  

---

### **🎯 总结**
iOS 开发常用的设计模式可以帮助我们**提高代码的可维护性、解耦不同模块、提升开发效率**，实际开发中应结合具体业务需求灵活选择！🚀